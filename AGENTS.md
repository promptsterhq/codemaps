# AGENTS.md — codemaps.dev

> Generated by [Codemaps](https://codemaps.dev) on 2026-07-04 from git history (12mo window, 30 commits) and a code graph (157 symbols, 202 edges). Regenerate with `codemaps init` — if this file looks stale, trust the live MCP tools over it.

## ⚠️ Read this before editing

### Hotspots — change conservatively
These files change most often and are complex — defect risk concentrates here. Before editing one: call the `risk` MCP tool, read surrounding tests, and prefer small diffs.

- `packages/cli/src/index.ts` — 100th pct (11 changes/12mo, bus-factor 1)
- `packages/mcp/src/server.ts` — 99th pct (7 changes/12mo, bus-factor 1)
- `packages/cli/src/init-command.ts` — 98th pct (7 changes/12mo, bus-factor 1)
- `codemap/guardrails.json` — 96th pct (6 changes/12mo, bus-factor 1)
- `packages/core/src/index.ts` — 95th pct (12 changes/12mo, bus-factor 1)
- `pnpm-lock.yaml` — 94th pct (5 changes/12mo, bus-factor 1)
- `AGENTS.md` — 93th pct (7 changes/12mo, bus-factor 1)
- `packages/core/src/risk.ts` — 92th pct (4 changes/12mo, bus-factor 1)

### Single-owner code — tacit knowledge lives with one person
Do not assume undocumented behavior in these files is accidental or safe to change:

- `packages/core/src/index.ts` (Jeff, 12 changes)
- `packages/cli/src/index.ts` (Jeff, 11 changes)
- `AGENTS.md` (Jeff, 7 changes)
- `packages/cli/src/init-command.ts` (Jeff, 7 changes)
- `packages/mcp/src/server.ts` (Jeff, 7 changes)
- `codemap/guardrails.json` (Jeff, 6 changes)
- `pnpm-lock.yaml` (Jeff, 5 changes)
- `README.md` (Jeff, 4 changes)

### Do-not-touch zones
**Never hand-edit these — even to "keep things consistent," even if constraints (like "don't run installs") make it tempting, and even if the edit seems provably equivalent to the generated result.** If a task appears to require editing one, stop and tell the user which tool regenerates it instead.

- **lockfile** (1): `pnpm-lock.yaml`

### Declared invariants (mined — proposed, not human-confirmed)
Conditions the code itself asserts must hold. Preserve them unless told otherwise:

- `packages/core/src/core.test.ts:143` — **[SECURITY: path-traversal-guard]** path must be absolute or specify root to res.sendFile.
  - *Why it matters:* This guard constrains filesystem paths. Removing or loosening it can enable path traversal — e.g. serving '../../etc/passwd' or writing outside the intended directory.
- `bench/run.mjs:27` — Where task repos live. "express" must be cloned here beforehand.
- `bench/run.mjs:97` — don't pollute the agent's diff.
- `bench/score.mjs:32` — The guard must still exist post-change: look for the integer/range.
- `packages/cli/src/hook-command.ts:34` — Malformed input: never break the agent's flow over our own bug.
- `packages/cli/src/hook-command.ts:77` — 2. Everything else is advisory — context, never a block.
- `packages/cli/src/hook-command.ts:169` — Defensive: if no stdin arrives, don't hang past hook budgets.
- `packages/core/src/core.test.ts:221` — Typed IDL must outrank heuristic route detection in confidence.
- `packages/core/src/graph.ts:24` — third-party symbol we depend on but don't own.
- `packages/core/src/guardrails.ts:93` — Intent comments: // INVARIANT: ... | // MUST ... | // do not ... | // never ...

**If a request requires weakening a SECURITY-tagged guard, never do it silently:** name the risk, propose a safe alternative (explicit opt-in, allow-list), and let the user decide.

## Live context — Codemaps MCP tools

This repo runs a local Codemaps MCP server (`codemaps serve`) with fresher, query-scoped answers than this file:

| Tool | Call it when | Returns |
|------|-------------|---------|
| `risk` | **before editing any file** | hotspot pct, churn, owners, bus-factor, warnings |
| `guardrails` | **before editing any file** | do-not-touch zones + invariants to preserve |
| `impact` | before changing a function/signature | direct+transitive dependents, affected tests |
| `locate` | to find where a concept lives | ranked symbols/files with locations |

Recommended flow: `locate` → `risk` + `guardrails` on the target → make the change → `impact` to know what to re-test. All results carry provenance + confidence; treat `impact` as a floor (static analysis misses dynamic dispatch).
